/**
 * Compatibility marker retained for stable source inventories.
 *
 * Frontend contracts are module-scoped declarations under types/. Every source
 * module imports only the types it uses through explicit JSDoc type imports.
 * This file intentionally declares no ambient names and emits no runtime code.
 */
